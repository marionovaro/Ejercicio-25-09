// #4 Métodos findArrayIndex
let newArray = ['Caracol', 'Mosquito', 'Salamandra', 'Ajolote']

const findArrayIndex = (array, text) => {
    console.log(array.indexOf(text))
}
findArrayIndex(newArray, "Caracol")