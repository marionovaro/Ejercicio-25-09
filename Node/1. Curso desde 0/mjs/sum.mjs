export const sum = (a, b) => {
    return a + b;
}

//! exportamos con export delante de la función que queremos exportar
//! PARA HACERLO ASÍ TIENE QUE SER MJS O CAMBIAR EL PACKAGE JSON A "TYPE": "MODULE"